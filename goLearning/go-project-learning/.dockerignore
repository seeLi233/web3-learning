# ==================== 版本控制 ====================
# 为什么排除 .git？
# → .git 目录包含所有历史提交，可能有几百 MB。发送到 Docker daemon 极慢，
#   而且镜像里也不需要 git 历史
.git
.gitignore
.gitattributes

# ==================== IDE 配置 ====================
# 为什么排除 IDE 配置？
# → .vscode/.idea 是开发工具的个人配置，与运行无关
.vscode/
.claude/
.idea/
*.swp
*.swo
*~

# ==================== Go 相关 ====================
# 为什么排除 vendor？
# → 项目用 go module，依赖从 go.sum 锁定，vendor 目录只在 vendor 模式下使用
vendor/

# 为什么排除二进制文件？
# → Go 编译产物可能是 Windows 的 .exe（你在 Windows 上开发），
#   而 Docker 里要编译 Linux 二进制。混在一起会出错
*.exe
*.test
*.out

# 为什么排除覆盖率文件？
# → go test 生成的覆盖率报告只用于 CI 分析，不给镜像
coverage.out
coverage.html

# ==================== 日志 ====================
# 为什么排除日志？
# → 本地开发日志可能有几百 MB，打到镜像里除了增加体积没任何作用
#   运行时容器自己会写日志到 stdout/stderr
logs/
*.log
*.log.*

# ==================== 数据 ====================
# 为什么排除数据库文件和缓存？
# → 本地测试数据不能混入镜像。容器启动时用 volume 挂载或数据库迁移脚本初始化
consul-data/
elasticsearch-data/
*.db
*.sqlite
tmp/
temp/
.cache/

# ==================== 环境变量 ====================
# 为什么排除 .env？
# → .env 含数据库密码等敏感信息。真正需要的环境变量在 docker-compose.yml 的 environment 里注入
.env
.env.*
!.env.example    # 但保留 .env.example 作为配置模板参考

# ==================== Docker ====================
# 为什么排除 Docker 相关文件？
# → Dockerfile 和 compose 文件是给 docker 命令用的，不进入镜像
Dockerfile*
docker-compose*.yml
.dockerignore

# ==================== 各服务 ====================
# 为什么按服务排除？
# → 每个服务的 Dockerfile 只复制自己需要的源码，其他服务的不应该进构建上下文
#   （但在当前架构下，common/ 共享包需要被复制，所以不能全部排除）
#   这里不是排除服务目录本身，而是排除各服务内部的不必要文件
**/logs/
**/node_modules/

# ==================== 文档 ====================
README*.md
docs/

# ==================== 文档 ====================